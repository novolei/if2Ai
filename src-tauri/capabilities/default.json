{
  "identifier": "default",
  "description": "Default permissions for If2Ai (includes invoke access to all #[tauri::command] handlers registered via generate_handler!, e.g. memory_session_set_enabled — Phase 8A.4, pinned_get / pinned_add / pinned_delete / pinned_reorder — Phase 8A.10 / v2 §0.5 Δ-11, and memory_compile_now / memory_compiled_read / memory_compiled_clear — Phase 8B.5 / v2 §0.5 Δ-11)",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "core:window:default",
    "core:window:allow-start-dragging",
    "dialog:default",
    "dialog:allow-open"
  ]
}
