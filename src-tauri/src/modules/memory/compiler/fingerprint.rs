//! Phase 8B.1 — placeholder for the fingerprint sidecar mechanism.
//!
//! Real implementation lands in slice 8B.2 per
//! `docs/exec-plans/active/phase-8b-memory-compiler-and-ticker.yaml`
//! (T-C2): MD5(keys.join("\n")) cache + `*.md.fingerprint` sidecar
//! files + `is_unchanged` predicate consumed by every `compile_*`.
#![allow(dead_code)]
