{
  "name": "if2ai",
  "version": "0.1.0",
  "description": "AI智能体桌面应用 - 基于Tauri + Rust + Svelte",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "@tauri-apps/api": "latest",
    "@tauri-apps/cli": "latest",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "latest",
    "@tauri-apps/plugin-fs": "latest"
  }
}
